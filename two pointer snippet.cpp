
long long head=-1
long long tail=0;
long long ans;

while(tail<n)
{
    while(head+1<n and  )
    {
        head++;
    }

    // process ans

    if(head>=tail)
    {   
        //process dependencies
        tail++;
    }
    else {
        tail++;
        head=tail-1;
    }
}
